node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
!.env.local
package-lock.json

# Keep the Go backend's embedded SPA placeholder — go:embed all:dist needs it
# committed, but the generic dist/ rule above would otherwise exclude it.
!backend/internal/dist/
!backend/internal/dist/index.html
