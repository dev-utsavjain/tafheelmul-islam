# The 9 platform-provided env vars (imagine.bo / Railway injects these):
DB_HOST=
DB_PORT=5432
DB_USER=
DB_PASSWORD=
DB_NAME=
DB_SCHEMA=
JWT_SECRET=
PORT=8080
ENV=production

# Extra secrets this service needs (set them on the Railway service too, or
# email silently no-ops — donations/messages still persist):
GMAIL_USER=
GMAIL_APP_PASSWORD=
